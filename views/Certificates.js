import React from "react";
import {
  Platform,
  StyleSheet,
  Text,
  View,
  Image,
  TextInput,
  Button,
} from "react-native";

export const Certificates = ({ navigation }) => {
  return <View></View>;
};
